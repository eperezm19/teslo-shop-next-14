"use client";

interface Props {
  images: string[];
  title: string;
}

export default function SlideShow({ images, title }: Props) {
  return (
    <div>
      <h1>Client Component</h1>
    </div>
  );
}
